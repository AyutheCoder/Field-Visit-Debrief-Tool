# Client environment variables
# The dev server proxies /api to the backend (see vite.config.ts),
# so no API URL is needed during development.

# Optional: override API base URL for production builds
VITE_API_BASE_URL=""
